import { SxProps } from "@mui/material";

export const PaperSxProps = {
    display: 'flex',
    p: "20px 20px",
    borderRadius: 4,
    justifyContent: 'center',
    flexDirection: 'column',
    alignItems: 'center'
} as SxProps